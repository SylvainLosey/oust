import 'package:flutter/material.dart';

class LiftsScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          Center(child: Text('Lifts Screen'))
        ],
      ),
    );
  }
}
